name: logging-principal
summary: "Logging frontend"
description: |
  Represents a thing that stores the logs from other units and
  provides a web frontend for log analysis.
requires:
    logging-client:
       interface: logging
provides:
    logging-ui:
       interface: http
