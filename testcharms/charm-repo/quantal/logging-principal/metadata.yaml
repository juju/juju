name: logging-principal
summary: "Logging test charm - principal, not subordinate"
description: |
    This is a longer description which
    potentially contains multiple lines.
provides:
    logging-client:
       interface: logging
requires:
    logging-directory:
       interface: logging
       scope: container
    info:
       interface: juju-info
       scope: container
