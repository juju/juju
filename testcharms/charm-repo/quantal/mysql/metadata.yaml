name: mysql
summary: "Database engine"
description: "A pretty popular database"
provides:
  server:
    interface: mysql
  server-admin:
    interface: mysql-root
  db-router:
    interface: db-router
requires:
  metrics-client:
    interface: metrics
