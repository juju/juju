#!/bin/bash
set -x

export PATH=$PWD/plugin:$PATH
unitstatus=$(status-get)

status-set $unitstatus "plugin-reset: resetting the plugin log"

result=$(juju-workload-testing-plugin reset 2>&1)
rc=$?
if [ "$rc" != "0" ]; then
    status-set $unitstatus "plugin-reset: failed ($rc - $result)"
fi
