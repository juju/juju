name: multi-series-subordinate2
summary: "That's a dummy subordinate charm with multi-series."
description: |
    This is a longer description which
    potentially contains multiple lines.
subordinate: true
provides:
    multi-client:
       interface: logging
requires:
    multi-directory:
       interface: logging
       scope: container
