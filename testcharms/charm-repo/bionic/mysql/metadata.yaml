name: mysql
summary: "Database engine"
description: "A pretty popular database"
provides:
  db:
    interface: mysql
  server:
    interface: mysql
  server-admin:
    interface: mysql-root
requires:
  metrics-client:
    interface: metrics
