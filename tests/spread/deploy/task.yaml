summary: Run deploy juju-qa-test with just a LXD provider
systems:
  - ubuntu-24.04
kill-timeout: 30m
environment:
  JUJU_MODEL: deploy

execute: |
  pushd "$PWD/../.."

  export PATH="$SPREAD_PATH/../go/bin:$PATH"

  ./spread.sh deploy test_deploy_charms 
