applications:
    ubuntu:
        charm: ubuntu
        scale: 1
        constraints: image-id={{IMAGE_ID}}

---

applications:
    ubuntu:
        base: ubuntu@22.04
        scale: 2
        constraints: image-id={{IMAGE_ID}}
