name: wordpress
revision: 3
summary: "Blog engine"
description: "A pretty popular blog engine"
provides:
  url:
    interface: http
    limit:
    optional: false
requires:
  db:
    interface: mysql
    limit: 1
    optional: false
  cache:
    interface: varnish
    limit: 2
    optional: true


