// Package sender contains constants used by both the client
// (cmd/juju/metricsdebug) and the workers (worker/metrics/sender). We put
// them here so that the client and workers don't have to depend on each other.
package sender
