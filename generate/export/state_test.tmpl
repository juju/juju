// Copyright 2025 Canonical Ltd. All rights reserved.
// Licensed under the AGPLv3, see LICENCE file for details.

package model

import (
	"testing"

	"github.com/juju/tc"

	schematesting "github.com/juju/juju/domain/schema/testing"
)

type exportStateSuiteV{{ .VersionToken }} struct {
	schematesting.ModelSuite
}

func TestExportStateSuiteV{{ .VersionToken }}(t *testing.T) {
	tc.Run(t, &exportStateSuiteV{{ .VersionToken }}{})
}

func (s *exportStateSuiteV{{ .VersionToken }}) TestExportRuns(c *tc.C) {
	st := NewState(s.TxnRunnerFactory())
	_, err := st.Export(c.Context())
	c.Assert(err, tc.ErrorIsNil)
}
