#!/bin/bash
set -eux
which juju
# Test upgrading an existing environment
$SCRIPTS/upgrade-juju $ENVS
for env in $ENVS; do
  $SCRIPTS/wait_for_agent_update.py $env
done
# Test deploying on the new version
$SCRIPTS/destroy-environment $ENVS
sleep 5
$SCRIPTS/deploy_stack.py $ENVS
